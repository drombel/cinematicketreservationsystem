{% extends 'base_panel.html.twig' %}

{% block body %}
    <h1>Ticket_sold creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('ticket_sold_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
